{
  "name": "airaquas-api-auth",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute airaquas-hair --file=../../shared/schema.sql",
    "db:remote": "wrangler d1 execute airaquas-hair --remote --file=../../shared/schema.sql"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^4.0.0"
  }
}
